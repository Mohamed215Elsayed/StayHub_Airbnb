export enum API_TAGS {
  AUTH = 'Auth',
  USERS = 'Users',
  COUNTRIES = 'Countries',
  CITIES = 'Cities',
  CURRENCIES = 'Currencies',
  UNIT_CATEGORIES = 'UnitCategories',
  APP_SETTINGS = 'AppSettings',
}
